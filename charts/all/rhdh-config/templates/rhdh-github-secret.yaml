apiVersion: "external-secrets.io/v1beta1"
kind: ExternalSecret
metadata:
  name: rhdh-github-secret
spec:
  refreshInterval: 15s
  secretStoreRef:
    name: {{ .Values.secretStore.name }}
    kind: {{ .Values.secretStore.kind }}
  target:
    name: rhdh-github-secret
    template:
      type: Opaque
  dataFrom:
  - extract:
      key: secret/data/hub/rhdh-keys